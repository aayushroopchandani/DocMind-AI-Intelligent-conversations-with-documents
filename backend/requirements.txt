fastapi
uvicorn
watchfiles
pydantic
python-dotenv

# New setup dependencies
motor
pymongo
cloudinary
# Required by FastAPI to parse multipart/form-data (PDF uploads).
python-multipart

# Spreadsheet import/export (XLSX read/write for the analysis workspace)
openpyxl>=3.1.5,<4

# Phase 9 native execution engine. The minor version is pinned deliberately:
# type coercion, null ordering and expression semantics can change between
# releases, and every execution records the version it ran under.
polars>=1.43,<1.44

# RAG pipeline
langchain
langchain-classic
langchain-openai
langchain-qdrant
langchain-community
langchain-text-splitters
langgraph>=1.0,<2.0
qdrant-client
pymupdf>=1.23.0
requests
numpy
scikit-learn
